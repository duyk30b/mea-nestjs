import { ApiPropertyOptional, IntersectionType, PickType } from '@nestjs/swagger'
import { Expose, Transform, plainToInstance } from 'class-transformer'
import { IsObject, ValidateNested } from 'class-validator'
import { LimitQuery, PaginationQuery } from '../../../../../_libs/common/dto/query'
import {
  ProductGroupFilterQuery,
  ProductGroupRelationQuery,
  ProductGroupSortQuery,
} from './product-group-options.request'

export class ProductGroupGetQuery {
  @ApiPropertyOptional({ type: String, example: JSON.stringify(<ProductGroupRelationQuery>{}) })
  @Expose()
  @Transform(({ value }) => {
    try {
      if (!value) return undefined // return undefined để không validate nữa
      const plain = JSON.parse(value)
      return plainToInstance(ProductGroupRelationQuery, plain, {
        exposeUnsetFields: false,
        excludeExtraneousValues: false, // không bỏ qua field thừa, để validate chết nó
      })
    } catch (error) {
      return error.message
    }
  })
  @IsObject({ message: ({ value }) => value })
  @ValidateNested({ each: true })
  relation: ProductGroupRelationQuery

  @ApiPropertyOptional({
    type: String,
    example: JSON.stringify(<ProductGroupFilterQuery>{ updatedAt: { GT: Date.now() } }),
  })
  @Expose()
  @Transform(({ value }) => {
    try {
      if (!value) return undefined // return undefined để không validate nữa
      const plain = JSON.parse(value)
      return plainToInstance(ProductGroupFilterQuery, plain, {
        exposeUnsetFields: false,
        excludeExtraneousValues: false, // không bỏ qua field thừa, để validate chết nó
      })
    } catch (error) {
      return error.message
    }
  })
  @IsObject({ message: ({ value }) => value })
  @ValidateNested({ each: true })
  filter?: ProductGroupFilterQuery

  @ApiPropertyOptional({
    type: String,
    example: JSON.stringify(<ProductGroupSortQuery>{ id: 'ASC' }),
  })
  @Expose()
  @Transform(({ value }) => {
    try {
      if (!value) return undefined // return undefined để không validate nữa
      const plain = JSON.parse(value)
      return plainToInstance(ProductGroupSortQuery, plain, {
        exposeUnsetFields: false,
        excludeExtraneousValues: false, // không bỏ qua field thừa, để validate chết nó
      })
    } catch (error) {
      return error.message
    }
  })
  @IsObject({ message: ({ value }) => value })
  @ValidateNested({ each: true })
  sort?: ProductGroupSortQuery
}

export class ProductGroupPaginationQuery extends IntersectionType(
  ProductGroupGetQuery,
  PaginationQuery
) { }

export class ProductGroupGetManyQuery extends IntersectionType(
  PickType(ProductGroupGetQuery, ['filter', 'relation', 'sort']),
  LimitQuery
) { }

export class ProductGroupGetOneQuery extends PickType(ProductGroupGetQuery, ['relation']) { }
